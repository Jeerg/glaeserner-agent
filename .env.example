# Kopieren nach `.env` (gleicher Ordner) und den eigenen Schluessel eintragen.
# Damit findet die Server-Variante (werkbank_server.py / start.bat) den Schluessel.
# Alternativ vor dem Start: set OPENAI_API_KEY=...   (Windows)
OPENAI_API_KEY=
